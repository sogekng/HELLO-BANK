Na classe transações temos que implementar os seguintes métodos:

- Método POST "Efetuar Saque"
Dentro deste método teremos algumas funções:
1) Cadastrar novo saque com uma condição (Se for conta corrente,
o cliente tem um limite que excede o seu saldo, se for conta poupança,
o cliente não tem um limite além do seu próprio saldo);

2) Após o cadastro, invocar a classe conta e retirar o valor de saque do saldo do cliente.

- Método POST "Efetuar Depósito"
Funções:
1) Cadastrar novo depósito;

2) Invocar a classe conta para adicionar o valor de depósito no saldo do cliente

- Método POST "Transferência"
Funções:
1) Aqui, o usuário irá informar o tipo de transação (transferência), o valor da transferência,
a data e o id da conta para qual ele fará a transferência (Desta forma este registro ficará na própria
conta de quem está efetuando a transferência e conseguiremos visualizar no extrato)

2) Invocar a classe conta para localizarmos pelo id da conta informada que irá receber a transferência
e adicionarmos o valor de transferência na conta de quem está recebendo.